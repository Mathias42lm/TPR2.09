{% extends 'base.html.twig' %}

{% block body %}
<form method="post" action="{{ path('app_tp2_univ_create') }}">
    <div class="mb-3">
        <label>Nom</label>
        <input type="text" name="nom" class="form-control" required>
    </div>
    <button type="submit" class="btn btn-primary">Créer l'université</button>
</form>
<p>{{ message }}</p>
{% endblock %}